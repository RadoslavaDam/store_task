# Store Task

A small React web store built as a course project.

The app demonstrates a simple store with three user roles: a master administrator who manages user accounts, power administrators who manage items and approve baskets, and clients who browse the store and place orders.

Setup and run instructions are added in a later commit.
